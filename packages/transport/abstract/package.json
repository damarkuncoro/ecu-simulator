{
  "name": "@ecu/transport-abstract",
  "version": "0.1.0",
  "description": "Abstract transport interface + factory",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "optionalDependencies": {
    "@ecu/transport-tcp": "workspace:*",
    "@ecu/transport-serial": "workspace:*",
    "@ecu/transport-ws": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test:unit": "jest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  }
}
